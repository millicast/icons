@font-face {
  font-display: block;
  font-family: "{{ name }}";
  src: {{{ fontSrc }}};
}

.ml-viewer-{{prefix}}::before,
[class^="ml-viewer-{{prefix}}-"]::before,
[class*=" ml-viewer-{{prefix}}-"]::before {
  display: inline-block;
  font-family: {{ name }} !important;
  font-style: normal;
  font-weight: normal !important;
  font-variant: normal;
  text-transform: none;
  line-height: 1;
  vertical-align: -.125em;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

{{# each codepoints }}
.ml-viewer-{{ ../prefix }}-{{ @key }}::before { content: "\\{{ codepoint this }}"; }
{{/ each }}
